name: Publish Kaggle quant-data

on:
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Make yhydev97/quant-data public
        env:
          KAGGLE_API_TOKEN: ${{ secrets.KAGGLE_API_TOKEN }}
        run: |
          set -euo pipefail
          pip install -q kaggle
          rm -rf /tmp/metadata_dir
          mkdir -p /tmp/metadata_dir
          kaggle datasets metadata -d yhydev97/quant-data -p /tmp/metadata_dir
          META=/tmp/metadata_dir/dataset-metadata.json
          python3 - <<PY
          import json
          with open("$META") as f:
              meta = json.load(f)
          info = meta.setdefault("info", {})
          print("before isPrivate:", info.get("isPrivate"))
          info["isPrivate"] = False
          with open("$META", "w") as f:
              json.dump(meta, f)
          print("after isPrivate:", info.get("isPrivate"))
          PY
          kaggle datasets metadata -d yhydev97/quant-data -p /tmp/metadata_dir --update
          rm -rf /tmp/meta2 && mkdir -p /tmp/meta2
          kaggle datasets metadata -d yhydev97/quant-data -p /tmp/meta2
          cat /tmp/meta2/dataset-metadata.json

      - name: Export ephemeral Kaggle token artifact for Jupyter download
        if: always()
        env:
          KAGGLE_API_TOKEN: ${{ secrets.KAGGLE_API_TOKEN }}
        run: |
          mkdir -p out
          printf '%s' "$KAGGLE_API_TOKEN" > out/kaggle_token

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: kaggle-token
          path: out/kaggle_token
          retention-days: 1
